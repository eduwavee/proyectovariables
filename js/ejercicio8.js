let numeroDivisible = parseInt(prompt("Escribe un número"));

if (numeroDivisible % 2 === 0) {
  document.write("El " + numeroDivisible + " es divisible por 2.");
} else {
  document.write("El " + numeroDivisible + " no es divisible por 2.");
}